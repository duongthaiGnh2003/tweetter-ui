export enum SiginOrSigup {
  Sigin = "Sign in",
  Sigup = "Sign up",
}
export enum PickeType {
  Day = "Day",
  Month = "Month",
  Year = "Year",
}

export enum PostViewMode {
  ForYou,
  Following,
}

export enum CanReply {
  Everyone,
  AccountsFollow,
  VerifiedAccounts,
  OnlyAccountsMention,
}

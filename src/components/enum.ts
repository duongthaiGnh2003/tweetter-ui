export enum SigninOrSignup {
  Signin = "Sign in",
  Signup = "Sign up",
}
export enum PickerType {
  Day = "Day",
  Month = "Month",
  Year = "Year",
}

export enum PostViewMode {
  ForYou,
  Following,
}

export enum CanReply {
  Everyone,
  AccountsFollow,
  VerifiedAccounts,
  OnlyAccountsMention,
}
